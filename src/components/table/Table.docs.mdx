import { Canvas, Controls, Meta, Story } from "@storybook/addon-docs/blocks"
import * as TableStories from "./Table.stories"

<Meta of={TableStories} />

# Table

A semantic HTML table with styled header, body, optional caption, and configurable border type. Supports single-border and double-border display variants.

## When to use

Use `Table` to display structured, relational data where rows and columns have logical meaning. For lists of similar items without column relationships use `List`.

## Accessibility (WCAG 2.2)

- The table has an accessible name via `<caption>` or `aria-label` / `aria-labelledby`
- Header cells use `<th>` with `scope="col"` or `scope="row"` to associate them with data cells
- The table element has an enclosing scrollable container so it does not overflow on narrow viewports without losing focus visibility

## ✅ Do

- Always supply a `caption` or `aria-label` describing the table's purpose
- Mark row headers with `scope="row"` when rows have a logical header cell

## ❌ Don't

- Don't use a `Table` for layout — use CSS Grid or Flexbox
- Don't omit column headers (`<th scope="col">`) even if they are visually hidden

## Design Tokens

Customise `Table` via CSS custom properties on `:root` or a scoped ancestor.

| Token                            | Default                                                     | Description                     |
| -------------------------------- | ----------------------------------------------------------- | ------------------------------- |
| `--pk-table-border-color`        | `var(--pk-color-border)`                                    | Row separator border colour     |
| `--pk-table-header-accent-color` | `var(--pk-color-brand)`                                     | First header cell accent colour |
| `--pk-table-row-hover-bg`        | `color-mix(in srgb, var(--pk-color-brand) 8%, transparent)` | Row hover/focus background tint |
| `--pk-table-caption-color`       | `var(--pk-color-muted)`                                     | Caption text colour             |
| `--pk-table-cell-fg`             | `var(--pk-color-fg)`                                        | Default cell foreground colour  |
| `--pk-table-padding-y`           | `var(--pk-space-xl)`                                        | Cell vertical padding           |
| `--pk-table-padding-x`           | `var(--pk-space-sm)`                                        | Cell horizontal padding         |
| `--pk-table-container-pb`        | `var(--pk-space-xl)`                                        | Root container bottom padding   |

## Examples

### Default

<Canvas>
  <Story of={TableStories.Default} />
</Canvas>

### With icons

<Canvas>
  <Story of={TableStories.WithIcons} />
</Canvas>

### Double type

<Canvas>
  <Story of={TableStories.DoubleType} />
</Canvas>

### Props

<Controls of={TableStories.Default} />
