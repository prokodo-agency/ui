import { Canvas, Controls, Meta, Story } from "@storybook/addon-docs/blocks"
import * as PostItemStories from "./PostItem.stories"

<Meta of={PostItemStories} />

# PostItem

A compact blog list entry showing a thumbnail, category chip, title, date, and a redirect link. Intended for use inside post archives or search results.

## When to use

Use `PostItem` inside a vertically stacked list of posts. For a larger card-style teaser use `PostTeaser`; for a widget sidebar use `PostWidget`.

## Accessibility (WCAG 2.2)

- The redirect link wraps the entire card or has a clearly labelled `aria-label` with the post title to avoid duplicate link text
- Images carry descriptive `alt` text from the CMS entry; decorative images use `alt=""`
- Date strings are wrapped in `<time dateTime="…">` for machine-readable semantics

## ✅ Do

- Supply a canonical redirect URL — never an empty `href`
- Provide a meaningful category label to orient the reader

## ❌ Don't

- Don't render `PostItem` without a title — it is the primary accessible label
- Don't truncate the title at fewer than two lines; readers need enough context

## Design Tokens

Key `--pk-*` tokens used by this component:

| Token                         | Purpose                                                   |
| ----------------------------- | --------------------------------------------------------- |
| `--pk-space-xs`               | Paragraph element margin                                  |
| `--pk-space-md`               | Read count left padding                                   |
| `--pk-space-lg`               | Grid gap (mobile), button top margin                      |
| `--pk-space-xl`               | Grid gap (desktop)                                        |
| `--pk-color-muted`            | Date, read count, and paragraph text color                |
| `--pk-radius-lg`              | Thumbnail image wrapper border-radius                     |
| `--pk-color-brand/accent/...` | Colored border, background tint, and shadow (via `color`) |

> **Image responsiveness:** The `image` prop renders via `object-fit: cover` — the thumbnail fills `100 %` of the media column width and a minimum height of `300 px` at all breakpoints.

## Color

The `color` prop applies a matching visual accent to the image card **and** the
title Headline simultaneously, keeping the component visually cohesive:

- **Card**: adds a colored `border`, a subtle `background-color` tint, and an
  ambient `box-shadow` using `color-mix()` — identical to other design-system
  components (Input, Select, etc.).
- **Headline**: falls back to the same color value when `title.color` is not
  explicitly set.

Available values: `inherit` · `primary` · `secondary` · `success` · `warning` · `error` · `info` · `white`

## Examples

### Default

<Canvas>
  <Story of={PostItemStories.Default} />
</Canvas>

### Colored

<Canvas>
  <Story of={PostItemStories.Colored} />
</Canvas>

### Props

<Controls of={PostItemStories.Default} />
