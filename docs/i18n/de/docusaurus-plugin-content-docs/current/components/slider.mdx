---
id: slider
title: Slider
sidebar_label: Slider
description: Range slider input with single value and range (min/max) modes, configurable step, and accessible keyboard control.
---

# Slider

`Slider` rendert ein mit prokodo Design-Tokens gestyltes `<input type="range">`. Unterstützt Single-Thumb- und Dual-Thumb-Bereichsmodi.


---

## Übersicht

```tsx
import { Slider } from '@prokodo/ui/slider'

<Slider
  name="volume"
  label="Volume"
  min={0}
  max={100}
  step={1}
  value={volume}
  onChange={setVolume}
/>
```

<StorybookEmbed id="prokodo-form-slider--default" height={240} />


---

## Import

```ts
import { Slider } from '@prokodo/ui/slider'
```

CSS:

```ts
import '@prokodo/ui/slider.css'
```

---

## Props

| Prop | Typ | Standard | Pflicht | Beschreibung |
|------|------|---------|----------|-------------|
| `name` | `string` | — | ✅ | Input `name` attribute. |
| `label` | `string` | — | — | Visible label above the slider. |
| `min` | `number` | `0` | — | Minimum value. |
| `max` | `number` | `100` | — | Maximum value. |
| `step` | `number` | `1` | — | Increment step. |
| `value` | `number \| [number, number]` | — | — | Controlled value; tuple for range mode. |
| `onChange` | `(value: number \| [number, number]) => void` | — | — | Called on thumb drag. |
| `disabled` | `boolean` | `false` | — | Deaktiviere the slider. |
| `showValue` | `boolean` | `true` | — | Show current value label. |
| `className` | `string` | — | — | CSS-Klasse am Root-Element. |

> Siehe [`src/components/slider/Slider.model.ts`](https://github.com/prokodo-agency/ui/blob/main/src/components/slider/Slider.model.ts) für den vollständigen `SliderProps`-Typ.

---

## AIC-Hinweis

Verwende im Anwendungscode immer den Standard-Importpfad:

```ts
import { Slider } from '@prokodo/ui/slider'
```

Eine separate Auswahl von `/client` oder `/lazy` ist im Consumer-Code nicht erforderlich.

AIC-Komponenten unterstützen außerdem ein `priority`-Flag für kritische Above-the-fold-Elemente.
Am sichtbarsten ist das bei `Image` (`loading="eager"` + `fetchpriority="high"`).

---

## WCAG-2.2-Status

| Kriterium | Bezeichnung | Status | Hinweis |
|-----------|-------------|--------|---------|
| **1.3.1** | **Info und Beziehungen** (A) | ✅ Erfüllt | Semantische Struktur (Überschriften, Listen, Labels, Landmarks) muss programmatisch via HTML oder ARIA vermittelt werden. |
| **1.4.1** | **Verwendung von Farbe** (A) | 🔍 Manuell prüfen | Farbe darf nicht das einzige Mittel zur Informationsvermittlung sein (z. B. Fehlerhinweise, Pflichtfeldmarkierungen). |
| **2.1.1** | **Tastatur** (A) | 🔍 Manuell prüfen | Alle Funktionalität muss allein über Tastatur bedienbar sein, ohne Timing-Anforderungen. |
| **2.4.7** | **Fokus sichtbar** (AA) | 🔍 Manuell prüfen | Auf jedem interaktiven Element muss ein sichtbarer Tastaturfokus-Indikator vorhanden sein. Prüfung gegen das angewendete Produkt-Theme erforderlich. |
| **2.5.3** | **Label im Namen** (A) | ✅ Erfüllt | Bei Komponenten mit sichtbarem Label muss der zugängliche Name den sichtbaren Labeltext enthalten oder diesem entsprechen. |
| **3.3.2** | **Bezeichnungen oder Anweisungen** (A) | ✅ Erfüllt | Jedes Formularfeld benötigt ein programmatisch verknüpftes Label oder eine sichtbare Anweisung. |
| **4.1.2** | **Name, Rolle, Wert** (A) | ✅ Erfüllt | Name, Rolle und Zustand aller interaktiven UI-Komponenten müssen via nativer HTML-Semantik oder ARIA programmatisch bestimmbar sein. |

> **Testabdeckung:** 2 jest-axe-Assertion(s) in 5 Testdatei(en) · 7 ARIA-Attribut-Vorkommen im Quellcode-Scan.
> Kriterien mit 🔍 erfordern manuelle Prüfung im finalen Integrations- und Theme-Kontext.

## Storybook

👉 [Slider in Storybook öffnen](https://ui.prokodo.com/storybook/?path=/docs/prokodo-form-slider--docs)

---

## Quellcode

[`src/components/slider/Slider.model.ts`](https://github.com/prokodo-agency/ui/blob/main/src/components/slider/Slider.model.ts)
