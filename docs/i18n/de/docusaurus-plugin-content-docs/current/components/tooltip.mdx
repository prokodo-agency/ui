---
id: tooltip
title: Tooltip
sidebar_label: Tooltip
description: Accessible tooltip component anchored to any trigger — unterstützt hover, focus, and click activation with configurable placement.
---

# Tooltip

`Tooltip` rendert kontextbezogene Hinweise bei Hover/Fokus und verbessert die Verständlichkeit kompakter UI-Elemente.


---

## Übersicht

```tsx
import { Tooltip } from '@prokodo/ui/tooltip'

<Tooltip content="This action cannot be undone" placement="top">
  <button>Delete</button>
</Tooltip>
```

<StorybookEmbed id="prokodo-feedback-tooltip--default" />


---

## Import

```ts
import { Tooltip } from '@prokodo/ui/tooltip'
```

CSS:

```ts
import '@prokodo/ui/tooltip.css'
```

---

## Props

| Prop | Typ | Standard | Pflicht | Beschreibung |
|------|------|---------|----------|-------------|
| `children` | `ReactElement` | — | ✅ | Trigger element — must be a single element that forwards a `ref`. |
| `content` | `ReactNode` | — | ✅ | Tooltip content. |
| `placement` | `"top" \| "bottom" \| "left" \| "right"` | `"top"` | — | Preferred placement relative to the trigger. |
| `trigger` | `"hover" \| "focus" \| "click" \| "both"` | `"both"` | — | What user interaction opens the tooltip. |
| `delay` | `number` | `200` | — | Open delay in milliseconds. |
| `disabled` | `boolean` | `false` | — | Deaktiviere tooltip display. |
| `className` | `string` | — | — | CSS class on the tooltip popover element. |

> Siehe [`src/components/tooltip/Tooltip.model.ts`](https://github.com/prokodo-agency/ui/blob/main/src/components/tooltip/Tooltip.model.ts) für den vollständigen `TooltipProps`-Typ.

---

## AIC-Hinweis

Verwende im Anwendungscode immer den Standard-Importpfad:

```ts
import { Tooltip } from '@prokodo/ui/tooltip'
```

Eine separate Auswahl von `/client` oder `/lazy` ist im Consumer-Code nicht erforderlich.

AIC-Komponenten unterstützen außerdem ein `priority`-Flag für kritische Above-the-fold-Elemente.
Am sichtbarsten ist das bei `Image` (`loading="eager"` + `fetchpriority="high"`).

---

## WCAG-2.2-Status

| Kriterium | Bezeichnung | Status | Hinweis |
|-----------|-------------|--------|---------|
| **1.3.1** | **Info und Beziehungen** (A) | ✅ Erfüllt | Semantische Struktur (Überschriften, Listen, Labels, Landmarks) muss programmatisch via HTML oder ARIA vermittelt werden. |
| **1.4.3** | **Kontrast (Minimum)** (AA) | 🔍 Manuell prüfen | Text benötigt ein Kontrastverhältnis von mindestens 4,5:1 (3:1 für großen Text). Prüfung im finalen Produkt-Theme erforderlich. |
| **2.4.7** | **Fokus sichtbar** (AA) | 🔍 Manuell prüfen | Auf jedem interaktiven Element muss ein sichtbarer Tastaturfokus-Indikator vorhanden sein. Prüfung gegen das angewendete Produkt-Theme erforderlich. |
| **4.1.2** | **Name, Rolle, Wert** (A) | ✅ Erfüllt | Name, Rolle und Zustand aller interaktiven UI-Komponenten müssen via nativer HTML-Semantik oder ARIA programmatisch bestimmbar sein. |

> **Testabdeckung:** 2 jest-axe-Assertion(s) in 5 Testdatei(en) · 3 ARIA-Attribut-Vorkommen im Quellcode-Scan.
> Kriterien mit 🔍 erfordern manuelle Prüfung im finalen Integrations- und Theme-Kontext.

## Storybook

👉 [Tooltip in Storybook öffnen](https://ui.prokodo.com/storybook/?path=/docs/prokodo-feedback-tooltip--docs)

---

## Quellcode

[`src/components/tooltip/Tooltip.model.ts`](https://github.com/prokodo-agency/ui/blob/main/src/components/tooltip/Tooltip.model.ts)
