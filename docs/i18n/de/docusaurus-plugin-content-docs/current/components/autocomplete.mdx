---
id: autocomplete
title: Autocomplete
sidebar_label: Autocomplete
description: Searchable input with suggestion dropdown, async loading, free-text mode, and multi-select support.
---

# Autocomplete

`Autocomplete` kombiniert ein Texteingabefeld mit einem gefilterten Vorschlags-Dropdown. Unterstützt statische Options-Arrays, asynchrones Laden aus Remote-Quellen, freie Texteingabe und Multi-Select mit Chip-Anzeige.

---

## Übersicht

```tsx
import { Autocomplete } from "@prokodo/ui/autocomplete"
;<Autocomplete
  name="city"
  label="City"
  options={cities}
  value={selectedCity}
  onChange={setSelectedCity}
  placeholder="Search cities…"
/>
```

<StorybookEmbed id="prokodo-form-autocomplete--default" />

---

## Import

```ts
import { Autocomplete } from "@prokodo/ui/autocomplete"
```

CSS:

```ts
import "@prokodo/ui/autocomplete.css"
```

---

## Props

| Prop           | Typ                                | Standard | Pflicht | Beschreibung                                               |
| -------------- | ---------------------------------- | -------- | ------- | ---------------------------------------------------------- |
| `id`           | `string`                           | —        | ✅      | HTML-id für den Input (für Barrierefreiheit erforderlich). |
| `items`        | `AutocompleteItem[]`               | —        | ✅      | Auswählbare Optionen.                                      |
| `label`        | `string`                           | —        | —       | Eingabe-Beschriftungstext.                                 |
| `placeholder`  | `string`                           | —        | —       | Platzhaltertext.                                           |
| `value`        | `string`                           | —        | —       | Gesteuerter Eingabewert.                                   |
| `defaultValue` | `string`                           | —        | —       | Unkontrollierter Ausgangswert.                             |
| `disabled`     | `boolean`                          | `false`  | —       | Eingabe deaktivieren.                                      |
| `required`     | `boolean`                          | `false`  | —       | Feld als Pflichtfeld markieren.                            |
| `fullWidth`    | `boolean`                          | `false`  | —       | An Container-Breite anpassen.                              |
| `color`        | `Variants`                         | —        | —       | Farbakzent-Variante.                                       |
| `helperText`   | `string`                           | —        | —       | Hilfetext unterhalb der Eingabe.                           |
| `errorText`    | `string`                           | —        | —       | Fehlermeldung (setzt Fehlerzustand).                       |
| `onChange`     | `(value: string) => void`          | —        | —       | Wird bei Texteingabe aufgerufen.                           |
| `onSelectItem` | `(item: AutocompleteItem) => void` | —        | —       | Wird bei Optionsauswahl aufgerufen.                        |
| `className`    | `string`                           | —        | —       | CSS-Klasse am Root-Element.                                |

> Siehe [`src/components/autocomplete/Autocomplete.model.ts`](https://github.com/prokodo-agency/ui/blob/main/src/components/autocomplete/Autocomplete.model.ts) für den vollständigen `AutocompleteProps`-Typ.

---

## Design-Tokens

Passe `Autocomplete` über CSS Custom Properties auf `:root` oder einem übergeordneten Element an.

| Token                             | Standard                         | Beschreibung           |
| --------------------------------- | -------------------------------- | ---------------------- |
| `--pk-autocomplete-bg`            | `var(--pk-color-surface)`        | Eingabe-Hintergrund    |
| `--pk-autocomplete-fg`            | `var(--pk-color-fg)`             | Textfarbe              |
| `--pk-autocomplete-radius`        | `var(--pk-radius-pill)`          | Eckenradius            |
| `--pk-autocomplete-shadow`        | `var(--pk-shadow-lg)`            | Dropdown-Schatten      |
| `--pk-autocomplete-gradient-from` | `var(--pk-color-brand)`          | Verlauf Start (Fokus)  |
| `--pk-autocomplete-gradient-to`   | `var(--pk-color-accent)`         | Verlauf Ende (Fokus)   |
| `--pk-autocomplete-item-hover-bg` | `var(--pk-palette-primary-50)`   | Item-Hover-Hintergrund |
| `--pk-autocomplete-dropdown-bg`   | `var(--pk-color-surface-raised)` | Dropdown-Hintergrund   |

## AIC-Hinweis

Verwende im Anwendungscode immer den Standard-Importpfad:

```ts
import { Autocomplete } from "@prokodo/ui/autocomplete"
```

Eine separate Auswahl von `/client` oder `/lazy` ist im Consumer-Code nicht erforderlich.

AIC-Komponenten unterstützen außerdem ein `priority`-Flag für kritische Above-the-fold-Elemente.
Am sichtbarsten ist das bei `Image` (natives Preloading via `<link rel="preload">` für Above-the-fold-Inhalte).

---

## WCAG-2.2-Status

| Kriterium  | Bezeichnung                            | Status            | Hinweis                                                                                                                                                      |
| ---------- | -------------------------------------- | ----------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **1.3.1**  | **Info und Beziehungen** (A)           | ✅ Erfüllt        | Semantische Struktur (Überschriften, Listen, Labels, Landmarks) muss programmatisch via HTML oder ARIA vermittelt werden.                                    |
| **1.3.5**  | **Eingabezweck identifizieren** (AA)   | 🔍 Manuell prüfen | Formularfelder, die persönliche Daten erfassen, müssen ihren Zweck via HTML-autocomplete-Attribut kommunizieren.                                             |
| **2.1.1**  | **Tastatur** (A)                       | ✅ Erfüllt        | Alle Funktionalität muss allein über Tastatur bedienbar sein, ohne Timing-Anforderungen.                                                                     |
| **2.1.2**  | **Keine Tastaturfalle** (A)            | 🔍 Manuell prüfen | Der Tastaturfokus darf niemals innerhalb einer Komponente eingeschlossen sein; Nutzer müssen immer mit Standardtasten navigieren können.                     |
| **2.4.3**  | **Fokusreihenfolge** (A)               | 🔍 Manuell prüfen | Die Tastaturfokus-Reihenfolge muss im vollständigen seitenseitigen Integrationskontext Bedeutung und Bedienbarkeit erhalten.                                 |
| **2.4.7**  | **Fokus sichtbar** (AA)                | 🔍 Manuell prüfen | Auf jedem interaktiven Element muss ein sichtbarer Tastaturfokus-Indikator vorhanden sein. Prüfung gegen das angewendete Produkt-Theme erforderlich.         |
| **2.4.11** | **Fokus nicht verdeckt (Min.)** (AA)   | 🔍 Manuell prüfen | Die fokussierte Komponente darf nicht vollständig durch Sticky-Header, Overlays oder andere positionierte Seitenelemente verdeckt sein.                      |
| **3.3.1**  | **Fehlererkennung** (A)                | 🔍 Manuell prüfen | Eingabefehler müssen in Textform identifiziert und dem Nutzer beschrieben werden, nicht nur durch Farbe.                                                     |
| **3.3.2**  | **Bezeichnungen oder Anweisungen** (A) | ✅ Erfüllt        | Jedes Formularfeld benötigt ein programmatisch verknüpftes Label oder eine sichtbare Anweisung.                                                              |
| **4.1.2**  | **Name, Rolle, Wert** (A)              | ✅ Erfüllt        | Name, Rolle und Zustand aller interaktiven UI-Komponenten müssen via nativer HTML-Semantik oder ARIA programmatisch bestimmbar sein.                         |
| **4.1.3**  | **Statusmeldungen** (AA)               | ✅ Erfüllt        | Statusmeldungen (Laden, Erfolg, Fehler, Fortschritt) müssen assistiven Technologien ohne Fokusverschiebung mitgeteilt werden (aria-live oder role='status'). |

> **Testabdeckung:** 2 jest-axe-Assertion(s) in 4 Testdatei(en) · 3 ARIA-Attribut-Vorkommen im Quellcode-Scan.
> Kriterien mit 🔍 erfordern manuelle Prüfung im finalen Integrations- und Theme-Kontext.

## Storybook

👉 [Autocomplete in Storybook öffnen](https://ui.prokodo.com/storybook/?path=/docs/prokodo-form-autocomplete--docs)

---

## Quellcode

[`src/components/autocomplete/Autocomplete.model.ts`](https://github.com/prokodo-agency/ui/blob/main/src/components/autocomplete/Autocomplete.model.ts)
